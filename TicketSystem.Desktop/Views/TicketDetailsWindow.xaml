<Window x:Class="TicketSystem.Desktop.Views.TicketDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TicketSystem.Desktop.Views"
        mc:Ignorable="d"
        Title="TicketDetailsWindow" Height="500" Width="500">
    <ScrollViewer>
        <StackPanel Margin="20">

            <TextBlock Text="Ticket Id:"/>
            <TextBlock x:Name="lblId"/>

            <TextBlock Text="Subject:"/>
            <TextBlock x:Name="lblSubject"/>

            <TextBlock Text="Description:"/>
            <TextBox x:Name="txtDesc" IsReadOnly="True" Height="80"/>

            <TextBlock Text="Priority:"/>
            <TextBlock x:Name="lblPriority"/>

            <TextBlock Text="Status:"/>
            <TextBlock x:Name="lblStatus"/>

            <TextBlock Text="Assigned To:"/>
            <TextBlock x:Name="lblAssigned"/>

            <!-- Admin Panel -->
            <StackPanel x:Name="adminPanel" Visibility="Collapsed">
                <TextBlock Text="Assign To"/>
                <ComboBox x:Name="cmbAssign"/>

                <TextBlock Text="Change Status"/>
                <ComboBox x:Name="cmbStatus">
                    <ComboBoxItem Content="Open"/>
                    <ComboBoxItem Content="InProgress"/>
                    <ComboBoxItem Content="Closed"/>
                </ComboBox>

                <TextBlock Text="Comment"/>
                <TextBox x:Name="txtComment" Height="60"/>

                <Button Content="Save" Click="Save_Click"/>
            </StackPanel>

            <!-- History -->
            <DataGrid x:Name="dgHistory" Margin="0,20"/>

        </StackPanel>
    </ScrollViewer>
</Window>
